package tasks

/*
import (
  "fmt"
  "testing"
  "hyperview.in/server/core/utils"
)


const (
  TEST_DB_USER = "apple"
  TEST_DB_PASSWORD = ""
  TEST_DB_NAME = "amp_db"
  TEST_REPO_NAME = "test_repo"
  TEST_DIR = "test_dir"
) 

func Test_CreateTask(t *testing.T) {

  mc := &mountConfig {
    target: "/workspace",
    mountType: "FUSE",
  }  

  mounts := make(map[string]*mountConfig)

  mounts["test_repo"] = mc
   

  taskConfig:= &TaskConfig {
    mounts: mounts,
    cmd: "who", 
    workDir: "/Users/apple/MyProjects/stash",
  }

  fake_db, _ := utils.FakeDb()
  tasker := NewShellTasker(fake_db)
  task_attrs, err := tasker.CreateTask(taskConfig)
  
  if err != nil {
    fmt.Println("failed to create task")
    t.Fail()
  }
  
  fmt.Println("task_attrs: ", task_attrs)

}

func Test_GetTask(t *testing.T) {
  
}
*/
 